name: Flow B

on:
  workflow_call:
    inputs:
      variable_b:
        description: 'example var'
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: ci
        run: |
          echo "${{ inputs.variable_b }}"
          env